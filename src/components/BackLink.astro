---
interface Props {
	href: string;
	label: string;
}

const { href, label } = Astro.props;
---

<a class="back-link" href={href}>← {label}</a>

<style>
	.back-link {
		display: inline-block;
	}
</style>
